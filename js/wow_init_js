wow = new WOW({
    boxClass: 'wow',      // 需要执行动画的元素的class
    animateClass: 'animate__animated', // animation.css动画的class
    offset: 1,            // 距离可视区域多少开始执行动画
    mobile: true,        // 是否在移动设备上执行动画
    live: true            // 异步加载的内容是否有效
});
wow.init();